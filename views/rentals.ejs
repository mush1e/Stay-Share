<section class="rentals">
        <% rentals.forEach(function(r) { %>
            <h3><%= r.cityProvince %></h3>
            <section class="featured-rentals">
            <% r.rentals.forEach(function(rental) { %>
                <%- include('partials/rental', {rental: rental}) %>
                <% }); %>
            </section>
        <% }); %>
</section>
